<h4>Activities</h4>
<ul>
  {{#each activity in controller}}
    <li>{{ activity.name }}</li>
  {{else}}
    <li>There are no activities present for this workout</li>
  {{/each}}
</ul>
{{controller}}
{{model}}